name: Pipeline
on:
    pull_request:
        branches:
            - main
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - name: install black
            run: pip install black


    
    test:
        runs-on: ubuntu-latest
        needs: build
        steps:
          - name: check if code is ok
            run: |
                if [$? != 0]; then
                 echo "there is a problem in the code"
    
    
    
